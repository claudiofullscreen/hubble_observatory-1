require "spec_helper"

RSpec.describe HubbleApiClient::Request do
  let(:valid_email) { "bruce.park@fullscreen.com" }
  let(:invalid_email) { "john-example.com" }

  it "#get_hubble_uuid" do
    pending('tbd')
  end

  it "#create_hubble_uuid" do
    pending('tbd')
  end
end
